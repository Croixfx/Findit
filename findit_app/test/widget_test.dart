// Placeholder — full widget tests to be added when Firebase test mocks are set up.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    expect(true, isTrue);
  });
}
